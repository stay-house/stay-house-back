package com.example.stay_house_back.dto.simulator.enums;

/**
 * 사용자가 구하는 임차 형태. loan_product.housing_target 과 대조한다.
 *
 * <p>이미 참조되고 있으나 리포에 없어 컴파일이 안 되던 파일이다.
 * 사용처에서 강제되는 값으로 복원했다.
 */
public enum HousingType {
    JEONSE,
    MONTHLY_RENT
}
